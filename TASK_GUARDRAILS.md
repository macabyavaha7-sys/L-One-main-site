# L-One 主站任务执行边界

每次修改前，先把任务拆成五项：

1. 目标：本次要解决的唯一问题是什么。
2. 可修改范围：允许改哪些文件、哪些数据行、哪些页面区块。
3. 不可修改范围：已经交付且与本任务无关的页面、样式、数据和脚本不动。
4. 预期可见结果：用户打开页面后应该看到什么变化。
5. 验证门槛：交付前必须跑哪些检查，检查哪些可见文字和交互。

默认权限规则：

- 内容修复任务：只允许改对应作品的 `metadata.json`、`assets/works/index.json` 中对应作品行、对应作品卡片/详情页渲染结果、必要的审查脚本和 `SITE_STATUS.md`。
- 版式修复任务：只允许改与该版式相关的 CSS/HTML/JS，不改作品正文、标题、标签、图片路径等内容数据。
- 批量导入任务：每条作品必须独立目录、独立 metadata、独立图片序列、独立索引行，不能混写。
- 全站生成逻辑、导航结构、路由规则、公开域名、部署配置属于高影响范围，必须明确得到用户同意后再改。
- 任何任务都不能把临时说明、工作目的、归档说明写进公开作品正文。
- 交互状态优先于版式状态：`hidden`、筛选结果、当前路由等交互状态不能被后续 CSS 版式规则覆盖。新增筛选或布局时，必须验证每个分类只显示对应类型作品。
- 作品类型是硬边界：`图文` 页面不得出现 `视频`，`视频` 页面不得出现 `图文`，`文章` 页面不得出现其它类型。不得为了保持排版丰满而跨类型补位。

交付前检查：

- 运行 `node scripts/site-audit.js`。
- 检查可见页面中不能出现 `??`、`???`、`�`、错误分隔符或工作说明文案。
- 检查 Works 分类筛选：每个可见作品卡片的 `data-work-type` 必须与当前筛选类型一致；`全部分类` 可以显示所有类型。
- 检查本次修改没有扩大到任务允许范围之外。
- 检查 `SITE_STATUS.md` 已记录更新时间和最近变更。
